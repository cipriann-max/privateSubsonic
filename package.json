{
  "name": "privatesubsonic",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted server that makes Internet Archive's free music browsable and playable in any OpenSubsonic client.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w server && npm run build -w web",
    "test": "npm run test -w server",
    "lint": "npm run lint -w server && npm run lint -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}